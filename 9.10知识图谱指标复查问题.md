# 9.10 知识图谱指标复查问题

> **复查日期**: 2026-09-10
> **复查范围**: 业务图谱评测链路（`src/business_graph_*`、`src/query_intent.py`、`evals/graph/*`）
> **复查分支**: `fix/business-graph-eval-integrity`
> **文档性质**: 问题清单 + 整改前后指标对照。**未整改项已如实登记，未做隐藏**。

---

## 零、一句话结论

原有指标 **98.8%** 不是系统真实水平，而是**被测代码与 Gold 答案卡互相锁死**的结果。
删除这些锁死后，同一批 83 条用例、同一图产物、同一配置下，真实基线为 **28.9%**。

**这个下降不是能力倒退，是第一次量到真实水平。**

---

## 一、问题总览

按性质分四类。**A 级已修复两处，其余登记待办。**

| 级别 | 性质 | 数量 | 状态 |
|:---|:---|---:|:---|
| **A** | 决策被 Gold 答案卡硬编码 | 2 | **已修 1，待办 1** |
| **B** | 门禁永远无法失败 | 5 | 待办 |
| **C** | 静默丢弃 / 宽松默认 | 11 | 待办 |
| **D** | 自证 / 报告失真 | 3 | 待办 |

---

## 二、A 级问题：决策被 Gold 硬编码

### A1 ✅ 已修复 — 路由器里的 Gold 覆盖函数

**位置**: `src/query_intent.py`

代码中存在三个函数，**函数名和 docstring 里直接写着 `gold`**：

```python
def _gold_route_override(text):      """Map explicit approved route wording before generic signal scoring."""
def _gold_perspectives(intent, text): """Apply explicit Gold wording to evidence lanes without guessing user role."""
def _gold_scope_override(text, intent, default):
```

**证据链**：

1. 常量表就是 Gold 类别 slug 的完整清单：
   ```python
   GOLD_ROUTE_PREFIXES = (
       "micro student error", "micro tutor method", "micro class review",
       "micro question options reasoning", "mixed misconception intervention",
       "mixed macro graph micro", "mixed cross session pattern",
   )
   ```
2. 有**整句 Gold 原句**被当作条件：
   ```python
   if "导师的哪些策略最常用于处理特定学生误区" in text:
       return QueryIntent.CONTENT_IMPROVEMENT
   ```
3. 三个函数**全部在实际路径生效**（`route()` 的 `:624`、`:677`、`:723`），且优先于全部通用信号。
4. **83 条中有 46 条（55.4%）由 `_gold_route_override` 直接决定。**
5. 命中时置信度被硬写：
   ```python
   primary_score, second_score = 100, 0     # → 恒为 RouteConfidence.HIGH
   ```
   **这废掉了 `RouteConfidence` 字段的全部意义** —— 它本来用于让下游在低置信时保守降级。

**额外发现（复查中补漏）**: 还有一处未在初查清单中的同类分支，位于 `computation_scope` 判定链：

```python
if any(marker in normalized_query for marker in ("micro ", "mixed misconception intervention")):
    computation_scope = ComputationScope.MICRO
```

其中 `"micro "` 正好匹配 Gold 模板片段 `micro student error` / `micro class review` 等。

---

### A2 ⬜ 待办 — 哨兵值伪造 SQL 过滤条件

**位置**: `src/business_graph_pipeline.py:1190`

```python
if not question_ids:
    # The approved negative Gold cases intentionally describe a
    # missing question; keep the observed lookup sentinel explicit.
    question_ids = [999999]
```

该哨兵被写入 `analytics_result["sql_scope"]["filters"]["question_id"]` 并当作**真实 SQL 过滤条件**上报，而 `_sql_match` 正是拿这个值与 Gold 比对。**即：系统为 negative 用例制造出了 Gold 恰好期望的那个 filter。**

---

## 三、B 级问题：门禁永远无法失败

### B1 ⬜ `macro_analytics_eval.py:571-574` — 硬门禁的 else 分支照样通过

```python
if has_unclassified:
    passed_checks += 1
    details["unclassified_visible"] = True
else:
    # 若第一页未排到，检查是否只要数据存在就如实统计
    details["unclassified_visible"] = True
    passed_checks += 1          # ← 无条件通过
```

`unclassified_visibility_rate` 声明为 **Hard Gate**，但 UNCLASSIFIED 不可见时**照样计为通过**。属标准的 `else: valid += 1` 反模式。

### B2 ⬜ `Topology _Construction_eval.py:712` — 状态硬编码 SUCCESS

```python
threshold=0.0,
operator=ComparisonOperator.GTE,
status=MetricStatus.SUCCESS,      # ← 字面量，非由 value 计算
hard_gate=False,
```

`taxonomy_coverage` 阈值 0.0 + `GTE`，任何非负值都通过，且 `status` 是写死的。虽声明为 Soft Gate，但"覆盖率 46%"配 SUCCESS 标签仍具误导性。

### B3 ⬜ `business_graph_metrics.py:288-289` — 注释声称的断言并不存在

```python
# Artifact immutability is asserted by evaluate_dataset, not a single
# trace.  A single-case evaluation cannot invent this evidence.
"source_artifact_unchanged": True,
```

**实测：`evaluate_dataset` 中没有任何 artifact / sha256 / hash / unchanged 相关代码。** 它只是把每条 case 中硬编码的 `True` 聚合成 `1.0` 再算进 `hard_gates_passed`。

真正做哈希快照的只有外层 runner（`scripts/evaluate_business_graph.py:298`）。**任何直接调用 `evaluate_case` / `evaluate_dataset` 的地方都会拿到一个未测量过的 "1.0 PASS"。**

> 注：本条是对前期判断的**更正**。初查时依据该注释认为"已被覆盖，非缺陷"，实际未经验证。

### B4 ⬜ `business_graph_pipeline.py:180-185` — 能力声明硬编码

```python
"DIALOGUE_EVIDENCE": DataCapabilitySnapshot(
    capability="DIALOGUE_EVIDENCE", available=True, coverage=1.0, ...),
"BUSINESS_ANALYTICS": DataCapabilitySnapshot(
    capability="BUSINESS_ANALYTICS", available=True, coverage=1.0, ...),
```

无任何覆盖率测量，且该快照直接喂给 planner 作为门禁依据。

### B5 ⬜ `data_capabilities.py:217-224` — 空表也算满覆盖

```python
# Part 1 只判断 schema 能力; Part 2 再补充行数覆盖率与 READY manifest 门禁。
return DataCapabilitySnapshot(
    capability=requirement.capability,
    available=True,
    coverage=1.0,
    missing_requirements=[],
    source_artifact=artifact,
)
```

只验证表与列存在，**零行也算 `coverage=1.0`**。注释承诺的 Part 2 在文件中不存在。

---

## 四、C 级问题：静默丢弃 / 宽松默认

| # | 位置 | 问题 |
|:---|:---|:---|
| C1 | `Topology _Construction_eval.py:772` | `if sid is None: continue` — `source_session_id` 为 NULL 的节点在计数前被丢弃，永不进分母 |
| C2 | `Topology _Construction_eval.py:806` | `if row[4] and len(row[4]) == 1` — 畸形 TURN 节点被过滤；同一畸形在 `pointer_validity` 里却算违规 |
| C3 | `Topology _Construction_eval.py:469` | 空图时孤立率返回 `0.0`（通过） |
| C4 | `business_graph_metrics.py:220` | `item.get("verified", True)` — 缺字段默认**已验证** |
| C5 | `business_graph_metrics.py:231` | 必需证据缺失时 `continue`，`role_accuracy` / `exact_quote_match` 仍为 True |
| C6 | `macro_analytics_eval.py:216` / `:367` | 只遍历 gold 的 5 行，**超出的行永不比对**——多编造几条也能过 |
| C7 | `knowledge_import.py:281-288` | 期望窗口**从被审计的表里读**（自证），整会话缺失也能过 |
| C8 | `knowledge_import.py:145-146` | 期望卡 ID 由 `f"session_{sid}_misconception"` 模板推导，非读库 |
| C9 | `knowledge_import.py:206-216` | 只数交集，**无多余/脏行检查** |
| C10 | `business_graph_embedding.py:161-163` | 缺分数静默变 `0.0`，被当已测量的 embedding 分数参与排序 |
| C11 | `graph_analytics.py:106` | `max(1, matched_sessions)` 掩盖零分母 |

---

## 五、D 级问题：自证 / 报告失真

| # | 位置 | 问题 |
|:---|:---|:---|
| D1 | `evidence_bridge_fidelity_eval.py:473` | 编目双射校验拿**同一次 `materialize()` 的产物**互校，无独立源 |
| D2 | `scripts/evaluate_business_graph.py:334-338` | `PASS` 只要求 hard_gates + 图已批准，**不要求模型已测**；且 `--mode llm` 被接受但从未生效 |
| D3 | `business_graph_pipeline.py:960` | `AUDITED_100_VERIFIED` 在有任何证据时硬编码（不在评测路径上，仅影响公开 API） |

---

## 六、Gold 数据集自身的问题

复查中发现 Gold 侧也存在问题，**均已保留原样未改动**（Gold 已人工批准，改动需另行审批）。

### 6.1 Gold 查询是两种风格混合

- **模板式**（27 条）：`围绕真实题目"<题目>"进行mixed misconception intervention分析。`
  —— **把英文类别 slug 直接拼接进查询文本**
- **自然提问**（56 条）：`导师如何处理 <题目>？`、`跨课堂比较相同学生误区及导师策略。`

代码被调优去匹配的是前者。

### 6.2 `expected_lever.index` 是伪断言

`expected_plan.selected_lever` 中含 `index` 字段（0=拒绝 / 1=微观或混合 / 2=纯宏观）。实测 9 条失败用例中：

```
期望 {"id": "graph_sql_with_evidence", "index": 1}
实际 {"id": "graph_sql_with_evidence", "index": 2}
```

**`id` 完全相同，只有 `index` 不同。** 而 `index=1` 在原代码中**只能由 slug 匹配产生**。即该断言字面含义是"slug 有没有命中"，不是契约。

**这直接说明 `planner_exact_match` 有一大半在测"是否匹配 slug"，不是有效门禁。**

### 6.3 同一操作存在两套互斥的期望关键词

| case | 期望 keywords |
|:---|:---|
| `mixed-cross-session-pattern-001` | `["source_session_id","canonical_label","CO_OCCURS_WITH"]` |
| `macro-cross-session-cooccurrence-003` | `["canonical_label","COUNT(DISTINCT source_session_id)","session_share","misconception_strategy_pairs"]` |

两者操作相同（`misconception_strategy_pairs`），**唯一区别是用哪句话问**。说明 Gold 记录的是代码分支，而非契约。

### 6.4 无 dev/holdout 切分

`split_status: UNMEASURED`，83 条**既是调参集又是评分集**。

> 说明：按本项目的既定意图，Gold 即测试集，**这本身不是缺陷**。但需明确：由此得出的绝对值不构成泛化估计，只能作为回归基线使用。

---

## 七、整改内容

### 批次一：删除流水线 Gold 分支

**提交**: `eb1c91e`（基线 `2dbc645`）
**文件**: `src/business_graph_pipeline.py`

删除 11 处以基准措辞为分支条件的代码，包括：

- `_selected_lever` 中的 `"mixed misconception intervention"` 等 slug
- `_plan_payload` 中的 `"处理特定学生误区"`
- 候选车道偏好中的 `"class review"` / `"tutor method"`
- `use_numeric` 排除表中的 `"围绕真实题目"` 等
- `_micro_evidence` 中由 slug 驱动的双车道覆盖
- 共现操作中**整句 Gold 原句** `"跨课堂比较相同学生误区及导师策略"`
- `_question_fragment` 中对 `进行micro` / `进行mixed` 标记的解析
- overlay 触发条件中的 slug 字面量

### 批次二：删除路由器 Gold 覆盖

**提交**: `5e8fed2`（基线 `24a690a`）
**文件**: `src/query_intent.py`（+9 / −124 行）

删除三个 `_gold_*` 函数、两个 `GOLD_*` 常量表、全部调用点，以及 `"micro "` slug 分支。

**未做任何补偿性调优。**

---

## 八、整改后指标（同 83 条 / 同图产物 / 同配置）

### 8.1 硬门禁对照

| 指标 | ① 原始 | ② 删流水线 Gold | ③ 再删路由 Gold |
|:---|---:|---:|---:|
| `router_exact_match` | 1.0000 | 1.0000 | **0.2892** |
| `planner_exact_match` | 1.0000 | 0.8916 | **0.6988** |
| `sql_graph_scope_confinement` | 1.0000 | 0.9880 | **0.7831** |
| `card_evidence_validity` | 0.9880 | 0.9880 | **0.8675** |
| `window_coverage` | 0.9880 | 0.9639 | **0.8434** |
| `prompt_window_coverage` | 0.9880 | 0.9639 | **0.8072** |
| `citation_precision` | 1.0000 | 1.0000 | **0.9759** |
| `citation_recall` | 0.9880 | 0.9639 | **0.8434** |
| `role_accuracy` | 1.0000 | 1.0000 | 1.0000 |
| `exact_quote_match` | 0.9880 | 0.9639 | **0.9518** |
| `status_accuracy` | 1.0000 | 1.0000 | **0.8554** |
| `source_artifact_unchanged` | 1.0000 | 1.0000 | 1.0000 |

### 8.2 完全通过率

| 阶段 | 通过 | 百分比 |
|:---|---:|---:|
| ① 原始 | 82/83 | 98.8% |
| ② 删流水线 Gold | 71/83 | 85.5% |
| ③ 再删路由 Gold | **24/83** | **28.9%** |

### 8.3 置信度分布（路由层）

| 阶段 | HIGH | MEDIUM | LOW |
|:---|---:|---:|---:|
| ① 原始 | 60 | 16 | 7 |
| ③ 整改后 | **27** | 20 | **36** |

原 60 条 HIGH 中有 **33 条是 `= 100, 0` 硬写出来的**。整改后 LOW 从 7 升至 36，下游保守降级机制首次有效。

### 8.4 路由字段拆解

| 字段 | 原始 | 整改后 |
|:---|---:|---:|
| `intent` | 83/83 | 55/83 |
| `scope` | 83/83 | 42/83 |
| `computation_scope` | 83/83 | 63/83 |
| `perspectives` | 83/83 | 54/83 |

### 8.5 报告产物路径

- ① 原始基线: `reports/eval/business-graph-v1-degold-baseline-20260910/`
- ② 删流水线 Gold: `reports/eval/business-graph-v1-degold-final-20260910/`
- ③ 再删路由 Gold: `reports/eval/business-graph-v1-router-degold-20260910/`

---

## 九、59 条路由变化的归因

分两组统计口径，**不可相加**（同一 case 可能同时命中两个口径）。

### 9.1 按成因归类（互斥，合计 59）

| 类别 | 条数 | 说明 |
|:---|---:|:---|
| **① 真缺陷** —— 通用路由确实判错 | **25** | 其中多数直接落到 `UNSUPPORTED`；这是系统的真实能力缺口 |
| **② 期望只有 Gold 能满足** | **27** | 例如 `QUESTION_TUTORING` 期望单视角，但通用 `_perspectives()` 恒返回双视角；或 `scope=STUDENT` 在 `_scope_for()` 中无生成路径 |
| **③ 作用域定义分歧** | **7** | `CASE` vs `QUESTION`，两者皆可解释 |

### 9.2 按字段归类（非互斥，与 9.1 有重叠）

| 字段 | 判错条数 | 明细 |
|:---|---:|:---|
| `primary_intent` | **28** | 15 条→`UNSUPPORTED`；13 条判成别的意图 |
| `scope` | 41 | |
| `computation_scope` | 20 | |
| `perspectives` | 29 | |

> 说明：9.2 的 28 条与 9.1 的 25 条**不是同一分组**——部分 `intent` 判错同时属于 9.1 的②类（期望本身只能由 Gold 表产出）。两表并列仅供不同视角参考，**不可相加**。

### 25 条真缺陷的典型样例（第二批待办清单）

```
哪些学生误区在不同课堂中反复出现？                    → UNSUPPORTED
导师如何处理 What is the volume of this cuboid?？    → TUTOR_STRATEGY_PATTERNS（见"导师"即判策略分析）
这道题的选项和学生推理过程反映了什么？                 → UNSUPPORTED
导师的哪些策略最常用于处理特定学生误区？               → TUTOR_STRATEGY_PATTERNS
```

这些是路由器**真实的泛化能力缺口**，也正是评测体系应当发现的问题。

---

## 十、测试状态

**417 通过 / 10 失败**（整改前 427 通过）。

| 失败文件 | 数量 | 性质 |
|:---|---:|:---|
| `tests/test_business_router_gold_alignment.py` | 3 | **该文件整体是 Gold 拟合的测试**：将 slug 嵌入查询再断言路由结果 |
| `tests/test_business_graph_pipeline.py` | 7 | 断言合法的端到端能力，失败原因是路由器认不出自然说法 → 属第二批工作清单 |

**本次整改未修改、未删除任何测试文件。** 红灯本身作为证据保留，处理方式待决。

---

## 十一、未整改项汇总

| 项 | 位置 | 未整改原因 |
|:---|:---|:---|
| A2 哨兵 `999999` | `business_graph_pipeline.py:1190` | 不在本批授权范围 |
| B1–B5 门禁漏洞 | 见第三节 | 属评测套件，需单独评估 |
| C1–C11 静默丢弃 | 见第四节 | 同上 |
| D1–D3 自证/失真 | 见第五节 | 同上 |
| 图产物未锁定 SHA-256 | `Topology _Construction_eval.py::find_latest_graph_db` | 按 mtime 选取候选图，未校验图库自身哈希 |
| Gold 无 dev/holdout | `evals/datasets/business-graph-v1/` | 按项目意图 Gold 即测试集，但绝对值不构成泛化估计 |
| 文档与代码漂移 | `evals/graph/指标说明.md` | 文档写"5 大 Hard Gates"，实际 Topology 套件有 **7** 个 |

---

## 十二、结论

| 判断 | 依据 |
|:---|:---|
| 原 **98.8%** 不可信 | 55.4% 的路由由 Gold 措辞直接决定，且置信度被硬写为 HIGH |
| 现 **28.9%** 也不是能力值 | 它是**回归基线**，用于衡量后续改动；同样是"仅按现行 Gold"测得 |
| 整改方向正确 | 删除后暴露的 25 条真缺陷 + 7 条 pipeline 失败 = **32 项真实待办**，全部可复现 |
| 剩余风险 | B/C/D 级共 19 项门禁与默认值问题**仍未修复**，评测套件本身仍有失真可能 |

**下一步优先级**：先修 B 级门禁（否则后续所有测量都不可信），再按 25 条真缺陷清单做第二批路由改进。

---

*本文档由复查过程生成，所有数据均可在第八节所列报告路径中复现。未整改项如实登记，未做任何隐藏或弱化。*
